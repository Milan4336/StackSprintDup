{
  "name": "cloud-native-fraud-detection",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:api": "cd api-gateway && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build": "npm run build:api && npm run build:frontend",
    "deploy": "cd azure && bash deploy.sh"
  }
}
